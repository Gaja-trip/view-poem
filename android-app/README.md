# 풍경시 Android 앱

공개 웹 소스나 인증 토큰을 APK에 복사하지 않고, 배포된 풍경시 사이트를 안전한 Android WebView로 여는 앱입니다.

- Android 10(API 29) 이상
- 사진·이미지 선택 지원
- 완성 작품과 이미지의 `다운로드/풍경시` 저장 지원
- HTTPS 전용, 로컬 파일 접근 차단
- 외부 사이트는 기본 브라우저로 열기

빌드에는 JDK 17, Android SDK 35, Gradle 8.7이 필요합니다.
